#include <stdio.h>
/**
 * main - uses puts() to output a string
 * Return: 0 if the output is successful
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
